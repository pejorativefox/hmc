[project]
name = "hmc"
version = "0.1.0"
description = "Python home-manager clone: declarative dotfiles, packages, user services, env from one TOML file"
readme = "README.md"
requires-python = ">=3.11"

[project.scripts]
hmc = "hmc:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
